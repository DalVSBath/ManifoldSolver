<Window x:Class="ManifoldSolver.Core.View.RunnerPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Running Analysis" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText" Text="Initialising..." Margin="0,0,0,5"/>

        <ProgressBar x:Name="ProgressBar" Grid.Row="1" Height="20" 
                     Minimum="0" Maximum="100" Value="0" Margin="0,0,0,10"/>

        <TextBox x:Name="LogBox" Grid.Row="2"
                 IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="11"
                 TextWrapping="NoWrap"
                 Background="#1E1E1E" Foreground="#D4D4D4"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="PauseButton" Content="Pause" Width="80" Margin="0,0,5,0" 
                    Click="PauseButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>